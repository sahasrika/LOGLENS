# LOGLENS
Log lens is a project where it uses aws resources like lambda , Dynamo db where each uploaded log is checked by the API and says the root cause of the issue and dependencies of the error. and suggests the changes In order to solve the issues and errors.
